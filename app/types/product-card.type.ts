export type ProductType = {
  title: string;
  images: string[];
  productInfo?: string;
  table?: string;
};

export type ProductCardType = {
  title: string;
  description: string;
  images: string[];
  productInfo?: string;
  table?: string;
};
